/**
 * Oracle — higher-reasoning Codex second opinion (read-only) with Pi UI.
 *
 * Load with: `pi -e extensions/oracle` (or the full path to this folder / `index.ts`,
 * depending on how your `pi` resolves extension entries).
 */
export { default } from "./logic";
